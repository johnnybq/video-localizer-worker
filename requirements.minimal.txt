# ==============================================================================
# TrafficPlant Video Localizer - MINIMAL requirements
# ==============================================================================
# Heavy ML packages (torch, transformers, etc.) are in the base image
# Models download to Network Volume on first run
# ==============================================================================

# RunPod SDK
runpod>=1.7.0

# HTTP & Storage
httpx>=0.27.0
boto3>=1.35.0
aiofiles>=24.1.0

# Video/Audio Processing
opencv-python-headless>=4.10.0
ffmpeg-python>=0.2.0
pydub>=0.25.1
soundfile>=0.12.0

# OCR (lightweight)
paddlepaddle>=2.6.0
paddleocr>=2.8.0

# Transcription
faster-whisper>=1.1.0

# Translation (fallback - main translation on server via Gemini)
argostranslate>=1.9.0

# Utilities
numpy>=1.26.0
pillow>=10.4.0
tqdm>=4.66.0
pydantic>=2.9.0
