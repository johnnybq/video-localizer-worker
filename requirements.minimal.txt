# ==============================================================================
# TrafficPlant Video Localizer - Dependencies
# ==============================================================================
# Base image (vastai/pytorch:2.10.0-cuda-13.0.2-py312) provides:
#   torch 2.10.0, torchvision 0.25.0, torchaudio 2.10.0,
#   transformers, diffusers, accelerate, xformers
# Python 3.12, CUDA 13.0
# ==============================================================================

# HTTP & Storage
requests>=2.32.0
httpx>=0.27.0
boto3>=1.35.0

# Video/Audio Processing
opencv-python-headless>=4.10.0
ffmpeg-python>=0.2.0
pydub>=0.25.1
soundfile>=0.12.0
decord>=0.6.0                    # Required by SAM2 video predictor (init_state)

# OCR — DeepSeek-OCR-2 (primary) + PaddleOCR (fallback)
paddlepaddle>=2.6.0
paddleocr>=2.8.0,<3.0.0
flash-attn>=2.6.0                   # Required by DeepSeek-OCR-2 for efficient attention

# Segmentation & Masking
sam2>=1.0

# Inpainting — VideoPainter (TencentARC)
# Requires custom diffusers fork + checkpoint from HuggingFace
# Installed separately in Dockerfile (git clone + pip install -e)
imageio>=2.35.0
imageio-ffmpeg>=0.5.1
kornia>=0.7.0
einops>=0.8.0
peft>=0.12.0
omegaconf>=2.3.0
safetensors>=0.4.0

# Transcription
faster-whisper>=1.1.0

# Audio Separation — demucs-infer: modern drop-in replacement for demucs
# Compatible with PyTorch 2.x and Python 3.12 (original demucs is abandoned)
demucs-infer>=4.1.0

# Voice Cloning (TTS)
f5-tts>=1.0.0

# Translation: REMOVED — done server-side via Gemini 3 Pro (TranslatorAgent)
# argostranslate was unreliable (no pre-downloaded language models, poor quality)

# Face Enhancement — monkey-patch for torchvision.transforms.functional_tensor
# compat applied in handler.py before importing gfpgan
gfpgan>=1.3.0
basicsr>=1.4.0

# Quality Assessment
pyiqa>=0.1.10

# Upscale
realesrgan>=0.3.0

# Replicate API (fallback for ProPainter inpainting)
replicate>=1.0.0

# RTL Text Rendering (Arabic, Hebrew, Farsi)
arabic-reshaper>=3.0.0
python-bidi>=0.6.0

# Utilities
numpy>=1.26.0
pillow>=10.4.0
tqdm>=4.66.0
pydantic>=2.9.0
